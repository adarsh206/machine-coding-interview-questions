import React from 'react'

const Notification = ({type = "info", message, onClose}) => {
  return (
    <div>
        {/* icon */}
        {/* message */}
        {/* close button */}
    </div>
  )
}

export default Notification